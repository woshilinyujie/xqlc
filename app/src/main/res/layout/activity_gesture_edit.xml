<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#212735" >

    <LinearLayout
        android:id="@+id/gesture_edit_top_layout"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:gravity="center"
        android:orientation="vertical" >

        <com.rongxun.xqlc.Lock.LockIndicator
            android:id="@+id/gesture_edit_indicator"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dip" />

        <TextView
            android:id="@+id/gesture_edit_tip"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:gravity="center_horizontal"
            android:text="@string/set_gesture_pattern"
            android:textColor="@color/colorWhite"
            android:layout_marginTop="10dip" />
    </LinearLayout>

    <TextView
        android:id="@+id/gesture_edit_bottom_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:gravity="center_horizontal"
        android:layout_marginTop="20dip"
        android:layout_marginBottom="20dp"
        android:text="@string/set_gesture_pattern_reason"
        android:textColor="@color/colorWhite" />
    <com.rongxun.xqlc.Lock.GestureEditViewGroup
        android:id="@+id/gesture_edit_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/gesture_edit_top_layout"
        android:layout_above="@+id/gesture_edit_bottom_text"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dip" >
    </com.rongxun.xqlc.Lock.GestureEditViewGroup>

</RelativeLayout>
